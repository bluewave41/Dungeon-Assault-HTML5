import MainMenu from '../scenes/MainMenu';
import Raider from '../models/Raider'

const Home = (props) => {
    return (
        <div id="container">
            <MainMenu />
        </div>
    )
}

export default Home;